{
  "name": "semi-auto-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Temporary cloud browser for iPhone-assisted note.com cookie sync.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "playwright": "^1.56.1",
    "zod": "^4.1.12"
  },
  "engines": {
    "node": ">=22"
  }
}
